import { PageShell } from "@/components/page-shell";

export default function ShopPage() {
  return (
    <PageShell
      title="Shop"
      description="Foundation for product grid, allergen filters, sorting, quick add, and personalized recommendations."
      nextStep="Integrate catalog schema + search index"
    />
  );
}
