import { PageShell } from "@/components/page-shell";

export default function CheckoutPage() {
  return (
    <PageShell
      title="Checkout"
      description="Prepared for one-page checkout, delivery slots, and secure multi-provider payment flow."
      nextStep="Integrate Paystack + Flutterwave adapters"
    />
  );
}
