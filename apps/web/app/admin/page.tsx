import { PageShell } from "@/components/page-shell";

export default function AdminPage() {
  return (
    <PageShell
      title="Admin Dashboard"
      description="Secure control plane placeholder for CMS, products, media, SEO, and operational analytics."
      nextStep="Stand up RBAC + CMS workspace"
    />
  );
}
