import { PageShell } from "@/components/page-shell";

export default function CartPage() {
  return (
    <PageShell
      title="Cart"
      description="Ready for persistent carts, promo logic, and upsell recommendations."
      nextStep="Add shared cart state + server sync"
    />
  );
}
