import { PageShell } from "@/components/page-shell";

export default function BlogPage() {
  return (
    <PageShell
      title="Blog"
      description="Editorial foundation for brand updates, nutrition insights, and campaign storytelling."
      nextStep="Connect CMS collections with SEO metadata"
    />
  );
}
