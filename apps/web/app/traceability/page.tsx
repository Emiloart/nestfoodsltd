import { PageShell } from "@/components/page-shell";

export default function TraceabilityPage() {
  return (
    <PageShell
      title="Batch Traceability"
      description="Entry point scaffold for batch code and QR-driven product journey visibility."
      nextStep="Connect batch lookup API + timeline UI"
    />
  );
}
